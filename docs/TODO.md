# TODO — Backlog

Future work, deferred items, and known gaps. **Check here before proposing new work** — it may already be tracked or deliberately deferred. When you finish something, check it off and log it in [`DEVLOG.md`](DEVLOG.md). When you discover or defer something, add it here with enough context to act on later.

Priority buckets: **Now** (do next) · **Next** (soon) · **Later** (someday) · **Housekeeping**. Format: `- [ ] Title — context / acceptance notes`.

---

## Now

- [x] **Apply migrations `00005`, `00006`, `00007`** — done 2026-07-15, all applied to the live DB (`hnkvgwizhlbdkugmwtef`) via the Supabase MCP connection and verified (the DB previously had only `00001`–`00004`). Still worth a live-app smoke-test of the `00005`/`00006` behavior changes. For `00005`: (a) like / comment / follow still create notifications under the tighter F11 policy; (b) a shared invite link works for a *second* person; (c) a public-community comment/like still succeeds. For `00006`: (d) a member can post to their community, a non-member cannot; (e) declining an invite still works (now via `decline_invite` RPC); (f) editing your own post/comment works; (g) a reply's parent must be on the same post. Until applied, F10–F13 and the 00006 fixes are inactive. (See DEVLOG 2026-07-14.)
- [x] **Close the July 14 RLS and storage audit gaps in a new `00006` migration** (done 2026-07-14; `00006_rls_integrity_fixes.sql`, **pending manual apply**). Covers: (a) invite metadata immutable to clients — invitee UPDATE policy dropped, decline moved to `decline_invite` RPC; (b) community post INSERT requires membership; (c) avatar Storage UPDATE now repeats the path/ext/MIME checks; (d) comment notifications bind `comment_id.post_id = post_id`; (e) targeted-invite acceptance only from `pending`. Verify each via a direct authenticated PostgREST request (test matrix in DEVLOG).
- [x] **Enforce `profiles.username` format at the DB** (done 2026-07-15; migration `00008`, applied + verified on the live DB via MCP). The `^[a-zA-Z0-9_]{3,30}$` rule was Zod-only (bypassable via direct PostgREST own-row `PATCH`), leaving the notification `mention` regex's `[a-zA-Z0-9_]` assumption unbacked. Added a `CHECK` (`NOT VALID` for a safe manual apply; enforced on all future writes) and made the `ensureProfileForAuthUser` nanoid collision-fallback `-`-free so it can't trip the constraint. Also re-asserted the `accept_invite(uuid)` execute grant in `00008` (00005/00006 redefined it via `CREATE OR REPLACE` without re-granting).
- [ ] **Make community-content images private.** `post-images` is a public Supabase bucket, so an invite-only post image remains available to anyone with its URL despite post RLS. Migrate it to private storage and have visible-post reads issue authorized URLs (or use an equivalent RLS-gated delivery path). Decide separately whether public avatars are intentional.
- [x] **Restore post/comment editing at the RLS boundary** (done 2026-07-14 in `00006`). Author-only `USING`/`WITH CHECK` UPDATE policies added for `posts` (WITH CHECK re-checks community membership) and `comments` (WITH CHECK mirrors the F10 post-visibility predicate).
- [x] **Enforce comment-parent integrity** (done 2026-07-14 in `00006`). Trigger `comments_parent_integrity` requires parent and child to share `post_id` and rejects self-parenting. Follow-up (optional UX): also validate the parent in `createComment` for a friendlier error before the DB rejects it.
- [x] **Complete and fail safely for the GDPR data export** (done 2026-07-14). `exportMyData` now surfaces the first query error (via `mapSupabaseUserMessage`) instead of returning `success` with empty arrays, and includes `notifications`, `invites_sent`, `invites_received`, and `communities_created`.
- [x] **Fix optimistic write-state regressions** (done 2026-07-14). `createComment` now returns the persisted `{ id, created_at }` and `CommentList` swaps the `temp-*` item for it, so a new comment is immediately editable/deletable/replyable. `toggleLike` now surfaces DELETE errors and `PostCard.applyLike` rolls back on a resolved `{ error }` (not just a thrown rejection).
- [x] **Align composer validation with its UI** (done 2026-07-14). `PostComposer.handleSubmit` now requires non-empty text before submit (matching `createPostSchema`), so an image-only post no longer fails after upload. Image stays optional alongside text. (Supporting text-optional image posts instead would need Zod + a DB `CHECK` change — deferred.)
- [x] **Repair invalid interactive markup and control labeling** (done 2026-07-14). Home CTAs are now `Link`s styled via a new `buttonClassName` helper (no `<a><button>`); the post body no longer wraps `RichText` in a `Link` (the timestamp is the post link instead), so mention links aren't nested anchors; the image double-tap target is a `<button>` with an `aria-label` (keyboard likes on Enter/Space via `e.detail === 0`, pointer keeps double-tap). Added labels: invite username input, community name/description/privacy, composer community select, and preference switches (`aria-label`).

## Next

- [x] **Paginate every growing collection and complete the community feed** (done 2026-07-14; migration `00007`). Keyset pagination (opaque `(created_at, id)` cursor, lexicographic `.or()` predicate → no skipped boundary rows) for the following feed, communities feed, `getPostsByUser`, `getPostComments` (by root; replies load with their roots), `getCommunityPosts`, `getCommunityMembers`, and followers/following. `/communities` now uses `InfiniteFeed` with a working load-more. Shared `useInfiniteList` hook + `lib/utils/cursor.ts`. Comment pagination fetches only the displayed roots + their direct replies. (Media tab still derives from loaded posts — grows as the Posts tab scrolls; noted as a minor limitation.)
- [x] **Remove redundant community and session reads.** *(Done 2026-07-15.)* `getCommunities` uses an embedded `community_members(count)` aggregate (no full member-row transfer), and the communities page passes the member ids it already read into `getCommunitiesFeed` so it doesn't re-query `community_members` on the same request; follow-state lists use a bounded `getFollowedSubset(ids)`. Final piece done 2026-07-15: `getCurrentUser` (and a new `getAuthUser`) are wrapped in request-scoped React `cache()`, the protected layout reuses `getCurrentUser` (so the layout + wrapped page share one `getUser()` + one `profiles` read instead of two each), and the unread-count query now runs in parallel with the profile read. No cross-request caching of personalized/RLS-protected data.
- [x] **Reduce personalized payload and hydration cost.** *(Done 2026-07-15.)* Replaced `profiles(*)` in post/comment/community-member/notification-actor relations with `(id, username, avatar_url)` and follower/following lists with `(id, username, avatar_url, bio)` — the exact rendered fields (drops `bio`+`created_at` per feed row). Server-rendered the settings sign-in email and the edit-profile initial data (no browser auth/profile round trips). Long feeds: `PostCard` is now `React.memo` (a "load more" no longer re-renders every mounted card) and each card's `<article>` uses `content-visibility:auto` + `contain-intrinsic-size` so off-screen cards skip render/layout. **Deliberately not done:** a full static-server / interactive-client split of `PostCard` — the inline edit and image double-tap-like entangle the "static" body with interactivity, so it's a broader redesign; memo + `content-visibility` is the low-risk alternative. Page-eviction of loaded feed pages also skipped (breaks scroll-up) in favor of the CSS approach.
- [x] **Stop mention autocomplete request fan-out.** *(Done 2026-07-15.)* `MentionTextarea` now stores the debounce timer in a ref and clears it before scheduling the next one and on unmount/close, so fast typing dispatches a single profile query instead of one per keystroke; the request-ID guard is kept for response ordering, and the lookup selects only `(id, username, avatar_url)`.
- [x] **Add indexes with the pagination work** (done 2026-07-14; `00007_pagination_indexes.sql`, **pending manual apply**). Added the composites for every new keyset shape: `posts(author_id, created_at desc)`, `posts(community_id, created_at desc)`, `comments(post_id, created_at)`, `comments(author_id)`, `community_members(community_id, created_at)`, `follows(following_id, created_at desc)`, `follows(follower_id, created_at desc)`, `notifications(user_id, created_at desc)`. Dropping duplicates was intentionally **not** done here — see the new Housekeeping item.
- [ ] **Drop now-redundant single-column indexes after production review.** The `00007` composites supersede several `00001` single-column indexes for their access paths (`idx_posts_author`, `idx_posts_community`, `idx_comments_post`, `idx_follows_follower`, `idx_follows_following`, `idx_community_members_community`, `idx_notifications_user`). `idx_profiles_username` / `idx_community_invites_token` may also duplicate unique-constraint indexes. Inspect `pg_stat_user_indexes` + constraint names in production, then drop only the confirmed-redundant ones in a new migration. Add trigram indexes for live `%term%` search only once scale warrants them.
- [x] **Parallelize independent server work.** *(Done 2026-07-15.)* Profile / followers / following pages load the viewer (`getCurrentUser`) and the viewed profile (`getProfile`) together; the post-detail page loads the post and its first comment page together; `createComment` reads the post author and parent-comment author in parallel and fires its comment-notification inserts alongside the mention fan-out. The community page's members+posts were already parallel. Dependent authorization checks (e.g. `getPost` needs the viewer id, community membership needs `post.community_id`) stay sequential, and notifications remain best-effort (RLS-gated, errors swallowed).
- [ ] **Community moderator management (make the `moderator` role usable).**
  Currently `moderator` exists in the schema + RLS (post/comment delete, `canModerate` UI) but **nothing can assign it** — this is intentional-for-now, not a bug. To ship it:
  - **RLS:** add a creator-only `UPDATE` policy on `community_members` — a community's creator may set a member's `role` to `member` or `moderator` (never `creator`). New migration, idempotent.
  - **Action:** `setMemberRole(communityId, userId, role)` in `lib/actions/community.ts` — auth-check, verify caller is the community creator, mutate, `revalidatePath`.
  - **UI:** promote/demote + remove controls in the members list (`components/community/community-tabs.tsx`), creator-only.
  - Consider: notify the promoted user; whether mods can remove/demote other mods (the current `community_members` DELETE policy lets any mod/creator remove anyone in the community — revisit if that's too broad once mods are real).
- [ ] **Invite link expiry + revocation.** Schema already has `expires_at` (unused). Let creators set an expiry when generating a link, and revoke a link (set status). Surface expiry in `get_invite_by_token`.
- [ ] **Block / mute users.** Needs a `blocks` table + RLS, filtering in feed/search/notifications, and UI. Privacy-relevant — design carefully.
- [ ] **Content reporting flow (DSA notice-and-action).** Report UI on posts/comments, a `reports` table, and reviewer tooling. Guidelines + Terms currently point to "report via email" as the interim. Compliance item for the EU audience.

## Later

- [ ] **Real-time updates** via Supabase Realtime (feed, notifications, comments).
- [ ] **Expo / React Native app** reusing `lib/` (types, validators, queries, actions). Swap `lib/supabase/server.ts` for a mobile client.
- [ ] **Rate limiting** on write actions (posts, comments, follows, invites) — abuse resistance.
- [ ] **Profile privacy settings** (the Settings "Privacy" toggles are currently device-local placeholders).
- [ ] **Direct messages** (optional, post-MVP).
- [ ] **Email notifications** (optional).
- [ ] **Community rules / guidelines display** on the community page.

## Housekeeping

- [ ] **Reconcile stale docs.** `README.md` and `PROJECT_OUTLINE.txt` predate the Editorial Ink redesign and migrations 00002–00005: they still say B&W/Geist, `middleware.ts` (now `proxy.ts`), "migration 00001" only, and list built features as future. Update or fold into a living `ARCHITECTURE.md`.
- [ ] **Fill legal placeholders** in `lib/legal/config.ts` (entity, contact, jurisdiction) and get the `/legal/*` template copy reviewed by counsel.
- [x] **Restore the lint gate** (done 2026-07-14). `npx eslint .` is clean (0 problems). Fixes: `join/page.tsx` derives the no-token error and keeps only async `setState` in its effect; `cookie-notice.tsx`, `preference-toggle.tsx`, and `theme.tsx` read their localStorage/`matchMedia` state via `useSyncExternalStore` (theme derives `resolved` and only writes the DOM class in an effect); `search-view.tsx` moves all `setState` into its debounce timers; and the unused `currentUser` prop was removed from `CommentRow`.
- [ ] **Supabase type generation** — generate types from the schema to replace / validate the hand-written `lib/types/database.ts`.
- [ ] **Image compression before upload** (avatars + post images) to cut storage + bandwidth.
- [x] Add the core docs set — `ARCHITECTURE.md`, `DECISIONS.md`, `SECURITY.md`, `DATABASE.md`, `SETUP.md` (done 2026-07-14).

---

## Recently done

Full history in [`DEVLOG.md`](DEVLOG.md). Latest: second-opinion review triage — `next` 16.1.6→16.2.10 (clears high App Router proxy-bypass/SSRF/CSRF CVEs), infinite-list state reset on filter/nav change, mention-notification post binding (migration `00009`, applied + verified on live DB), follow revalidation fix, composer rejected-file + mention-autocomplete Escape fixes, and tabs keyboard a11y (2026-07-15). Before that: pre-merge review fixes — `profiles.username` DB `CHECK` (`00008`) backing the notification-regex assumption, `accept_invite` grant re-asserted, `-`-free nanoid fallback, and doc-drift corrections (2026-07-15). Before that: performance pass — request-memoized `getCurrentUser`/`getAuthUser`, parallelized independent page + notification reads, narrowed embedded `profiles(*)` selects, mention-search debounce fix, server-rendered settings data, and `PostCard` memo + `content-visibility` for long feeds (2026-07-15). Before that: a scalable Mazi wordmark favicon (2026-07-15); keyset pagination + composite indexes (migration `00007`) (2026-07-14).
